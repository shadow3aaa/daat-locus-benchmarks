[project]
name = "daat-locus-benchmarks"
version = "0.1.0"
description = "Benchmark runner for Daat Locus."
readme = "README.md"
authors = [
    { name = "shadow3", email = "shadow3aaaa@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
daat-locus-benchmarks = "daat_locus_benchmarks:main"

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
